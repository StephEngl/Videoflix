services:
  web:
    build:
      context: .
      dockerfile: backend.Dockerfile
      target: development
    volumes:
      - ./:/app:cached
      - videoflix_media:/app/media
      - videoflix_static:/app/static